# Shared Package Space

This directory will hold shared schemas, API contracts, and cross-service conventions as the platform grows. In Phase 1 it serves as the agreed contract boundary for data shapes that will be reused by the API, agent, and streaming pipeline.

